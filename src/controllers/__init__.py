"""
Controllers package — business logic layer.

Each controller receives datasource(s) and connector(s) via dependency injection.
Contains orchestration logic: validation, date calculations, stage advancement rules,
event publishing, etc. One file per API resource.
"""
