"""
Connectors package — external service calls.

Connectors encapsulate communication with other microservices and
infrastructure services (event bus, IAM, file storage, etc.).
Each connector is injected into controllers that need it.
"""
