"""
Routes package — FastAPI routers, HTTP layer only.

Each file defines an `APIRouter` for one API resource.
Routes handle request/response parsing and delegate to controllers.
No business logic — just HTTP concerns (status codes, headers, path params).
"""
