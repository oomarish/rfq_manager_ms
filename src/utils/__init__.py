"""
Utils package — shared helpers used across all layers.

Modules:
- pagination  — page/size/sort parameter parsing, paginated response builder
- errors      — custom exception classes (NotFoundError, BadRequestError, etc.)
- uuid        — UUID generation helpers
"""
